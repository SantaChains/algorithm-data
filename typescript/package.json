{
  "name": "algorithm-data-typescript",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/regex.ts",
    "build": "bun build src/regex.ts --outdir dist",
    "typecheck": "npx tsgo --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260503.1",
    "bun-types": "^1.3.13"
  }
}
